interface:
  display_name: "Git Commit Messages"
  short_description: "Best commit message practices for useful history."
  default_prompt: "Write a commit message for these staged changes."

policy:
  allow_implicit_invocation: true
