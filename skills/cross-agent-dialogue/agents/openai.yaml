interface:
  display_name: "Cross-Agent Dialogue"
  short_description: "Review or discuss work via verified file-based agent turns."
  default_prompt: "Set up a file-based exchange with another agent to review or discuss this."

policy:
  allow_implicit_invocation: true
